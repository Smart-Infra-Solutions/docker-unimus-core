# Force LF line endings for YAML files (required for Woodpecker CI)
*.yaml text eol=lf
*.yml text eol=lf

# Force LF for shell scripts
*.sh text eol=lf

# Default behavior for other text files
* text=auto
